@model IEnumerable<OnlineStore.Models.Product.Size>


@foreach (var size in Model)
{
    <div class="form-group">
        @Html.DisplayFor(modelItem => size.SizeName)
        <div class="col-md-10">
            @Html.EditorFor(modelItem => size.SizeName, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(modelItem => size.SizeName, "", new { @class = "text-danger" })
        </div>
    </div>

    @*<div class="form-group">
        @Html.DisplayFor(modelItem => size.SizeAmount)
        <div class="col-md-10">
            @Html.EditorFor(modelItem => size.SizeAmount, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(modelItem => size.SizeAmount, "", new { @class = "text-danger" })
        </div>
    </div>*@
}
